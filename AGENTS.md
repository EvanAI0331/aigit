# AGENTS.md

记住：机制必须是 agent/LLM + 脚本共同执行，agent 依靠 skill 执行。

- 严禁把 agent 当傀儡，用硬编码关键词脚本做核心执行主体。
- 严禁硬编码写死、降级兜底、伪成功态、伪完成态。
- agent 核心逻辑必须由 LLM 驱动。
- 没有 LLM 支撑的，严禁称为 agent。
- 约束类内容写入对应 agent spec。
- 方式、技巧、策略、能力类内容写入对应 agent skill。
- 脚本只负责事实采集、契约校验、状态推进、落库、调度。

